import { Table } from 'reactstrap'
import { Link } from 'react-router-dom' 

export default function Card() { 

    // const {productSession, dispatch} = useContext(productSessionContext);
    //   getAll
    // delete
    // add
    // const deleteProduct = ( productId)=>{
    //     dispatch({
    //         type: TYPE.DELETE,
    //         payload: productId
    //     })
    // }

  return (
    // productSession.map(item=>{
        // <CardItem>

        // </CardItem>
    // })

    <h2>heelo</h2>
  )
}
